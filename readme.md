# Dotfiles

To use this repo clone it into your home directory.

Install the util `stow`.

In the root of the project run `stow ...` to setup symlinks, i.e. `stow nvim`.

The directories in the root of the project are the avaliable packages to stow.

